import Button from '@restart/ui/esm/Button'
import React from 'react'

function Buttons() {
    return (
        <div>
            <Button as={Link} to ='/Re'></Button>
        </div>
    )
}

export default Buttons
